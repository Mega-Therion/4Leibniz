[project]
name = "four-leibniz-runtime"
version = "0.2.0"
description = "Real-time verification gateway for 4Leibniz"
requires-python = ">=3.10"
dependencies = ["flask>=3.0", "websockets>=14.0", "cryptography>=42.0"]

[project.scripts]
leibniz-ws = "realtime:main"
